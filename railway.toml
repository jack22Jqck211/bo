[build]
builder = "nixpacks"
buildCommand = "pip install fastapi uvicorn[standard] psutil"

[deploy]
startCommand = "python main.py"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"